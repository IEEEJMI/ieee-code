#include<iostream>

using namespace std;

int main() {
    int N, M, S;
    cin>>N>>M>>S;

    int t=0;

    // cout<<( (N*M) + ((N-1)*S) );
    int arr[N];
    for(int j=0; j<N; j++) {
        arr[j] = S;
    }
    arr[0] = 0;
    arr[1] = 0;

    for(int k=1; k<N; k++){
        
    }

    return 0;
}